#include <stdio.h>

//Explain step by step evaluation of 3*x/y - z+k where x=2, y=3, z=3, k=1

int main(){
    //  int x=2, y=3, z=3, k=1;
    //  float a= 3*x/y - z+k;
    //  printf("%.0f \n", a); 

    //  // 3 * 2 / 3 - 3 + 1
    //  // 6 / 3 - 3 + 1
    //  // 2 - 3 + 1
    //  // -1 + 1
    //  // 0

    // 3.01 + 1 will be int, float, char?

    // float a = 3.0;
    // int b = 1;
    // float c = a+b;
    // printf("Value: %.1f \n", c);

    int x = printf("Hello");
    printf("%d \n", x);

    return 0;
}
#include <stdio.h>

int main(){
     int k=3.0/9;
    //  float b=2.0, c;
    //  c=a/b; 

     printf(" The value of c: %d \n", k); //The answer is 0.333 but it's can't store float so it's
    return 0;
}